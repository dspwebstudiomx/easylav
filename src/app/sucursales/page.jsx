import React from 'react'
import SucursalCard from '@/components/cards/SucursalCard'
import Container from '@/components/containers/Container';
import Section from '@/components/templates/Section';
import { localservices } from '@/app/data/sucursales'
import Spacing from '@/components/layout/Spacing';
import CityCardGenerator from './components/CityCardGenerator';

export const metadata = {
  title: "Sucursales | EASYLAV",
  description: "Generated by create next app",
};
console.log(localservices)

const Morelia = localservices.filter(localservice => localservice.ciudad === 'Morelia')
const Leon = localservices.filter(localservice => localservice.ciudad === 'León')

const Sucursales = () => {
  return (
    <Section>
      <Container>
        <h1 className='text-4xl text-center font-semibold'>Sucursales</h1>
        <Spacing height={'h-12'} />
        <h2 className='text-3xl'>Morelia</h2>
        <CityCardGenerator city={'Morelia'} />
        <Spacing height={'h-20'} />
        <h2 className='text-3xl'>León</h2>
        <CityCardGenerator city={'León'} />
      </Container>
    </Section>
  )
}

export default Sucursales